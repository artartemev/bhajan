# Облегчённый ML-стек: РАБОТАЕТ на Python 3.13 (без TensorFlow и madmom).
# Даёт реальное разделение дорожек, MIDI вокала и фисгармони, аккорды —
# алгоритмами на librosa. Качество грубее, чем у полного стека, но ставится везде.
# Требует ffmpeg в системе (для yt-dlp и чтения mp3).
demucs>=4.0          # разделение на стемы (vocals/drums/bass/other)
librosa>=0.10        # pyin (вокал), CQT (фисгармонь), хрома (аккорды)
soundfile>=0.12      # чтение/запись wav
scipy>=1.11          # band-pass для фисгармони
yt-dlp>=2024.0       # скачивание звука с YouTube
numpy>=1.24
faster-whisper>=1.0  # forced alignment текста по аудио (опционально)
indic-transliteration>=2.3  # романизация деванагари/бенгали для сравнения с текстом
